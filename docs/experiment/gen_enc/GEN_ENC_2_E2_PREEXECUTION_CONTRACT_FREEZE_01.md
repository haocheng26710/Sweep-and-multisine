# GEN-ENC-2 E2 preexecution contract freeze 01

Status: `FAIL_CLOSED_SCIENTIFIC_SEMANTIC_GAPS`; E2 execution is not authorized.

## Additive scope

This sidecar freezes the exact Final92 identities, the accepted optimized Route A runtime, the complete E2 workload, estimator and decision rules, two-stage data isolation, response persistence, resource stops, and independent-verifier obligations. It is additive to GEN-ENC-0R, GEN-ENC-1, GEN-ENC-2A REV01, GEN-ENC-2C, Final92, and timing Freeze04 runtime CORR01. It changes no older report, failure, terminal, result, research conclusion, or evidence level.

The evidence ceiling remains reduced-model E2 response evidence. It is not COMSOL, full-wave, manufacturability, or physical evidence. No formal response, development response, validation response, held-out response, or final-test data was read. No formal run ID was created.

## Frozen identity and workload

The global order is B1 HAND_01..20, B2 NEAR_01..20, B3 RANDOM_01..20, then B4 PHYSICS_01..20. Every slot is retained with its source path, source-file SHA-256, integrated identity-file SHA-256, and member-identity SHA-256. Reselection, replacement, redraw, repair by performance, or slot deletion is forbidden. HAND_01 remains only the timing proxy and receives no scientific priority.

The workload is 80 candidates; development angles 0/90/180/270; validation angles 0:15:345 with 45/135/225/315 mandatory; 3,675 nuisance cells, two repeats, four ports, and 256 computed frequencies. The primary endpoint uses n=0..207, exactly 200..3973.94499863515 Hz; the separate secondary sensitivity uses n=0..255, exactly 200..7947.8899972702975 Hz. There are 1,176,000 candidate-partition complete units and 16,859,136,000 complex values when ports are counted.

## Two-stage isolation

E2-D must finish all 80 candidates and seal source hashes, development responses, the common whitener, processing rules, candidate/family aggregates, and every allowed development decision before the validation root becomes readable. E2-V is a single-use transition. A validation failure cannot reopen development, change any identity, frequency, threshold, hyperparameter, family decision, or cost rule, and the same validation cannot be presented as another independent validation.

Formal complex responses, when separately authorized, are persisted as little-endian complex128 NPY v1.0 chunks with JSON sidecars, one candidate/partition/cell-chunk at a time. Writes use a sibling temporary file, flush and fsync, then atomic replace. Development and validation have disjoint roots and manifests. Canonical reduction order is family, member 01..20, partition, angle, cell 0000..3674, repeat 0..1, port 0/90/180/270, frequency n=0..255. Final-test paths are outside every allowlist.

## Scientific and terminal rules

Shared and differential responses use P_shared=11T/4 and P_diff=I-P_shared before whitening. The common W uses development repeat residuals only, OAS shrinkage to tau I, the frozen eigenvalue floor, and no feature deletion. E_primary is the noninterpolated weighted empirical Q0.05 of sigma3(WY_diff); r_stable counts the first three corresponding quantiles strictly above 1.0. Candidate PASS requires E_primary>1.0 and r_stable=3; equality fails.

Every candidate retains an applicable PASS, NEGATIVE, UNAVAILABLE, COST_INELIGIBLE, or TECHNICAL_FAILURE terminal. Family evidence is the minimum/worst-member result over the exact 20 members; a missing member makes the family unavailable. Global ranking is forbidden before four complete family terminals. The sampled bridge, mandatory held-out angles, matched-cost eligibility, and descriptive throughput diagnostic are reported without classifier substitution, frequency selection, threshold tuning, or member deletion. Primary and secondary results remain separate.

## Exact fail-closed gaps

Four authority gaps prevent E2 execution:

1. The hash-frozen optimized core rejects any state-angle tuple other than 0/90/180/270, while validation requires all 24 sampled angles. No authoritative mapping from a nominal validation angle to the core inputs is frozen.
2. The same core hardcodes development repeat seeds 2026091001/2026091002. No hash-frozen call path routes validation seeds 2026092001/2026092002 without changing source semantics.
3. The throughput formula names H_through_reference, but no authoritative reference identity, source path, or SHA-256 is frozen.
4. The estimator freezes equal nuisance-cell and within-cell residual weighting but does not uniquely specify the relative pooling weights among the 80 candidates and four families for the single common W.

Closing any gap requires an additive top-level scientific decision and a new preexecution freeze. This package does not choose an interpretation.

## Exclusions and resources

This contract does not authorize E2, inverse design, parameter search, extra members or families, COMSOL, full-wave/E3, physical experiment/E4, printing, paid services, legacy recomputation, or final-test. Hard stops remain 20 core-hours, 10 wall-hours, and 8 GiB aggregate peak. Single process is preferred. Parallelism remains disabled unless a later additive authority freezes it; any permitted worker count is at most eight with OMP, MKL, and OPENBLAS threads each set to one. Exceeding a cap produces RESOURCE_BLOCKED without workload contraction.
