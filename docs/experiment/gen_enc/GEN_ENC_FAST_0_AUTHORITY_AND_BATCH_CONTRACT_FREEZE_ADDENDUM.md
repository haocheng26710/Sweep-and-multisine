# GEN-ENC FAST-0 authority binding and four-batch contract freeze addendum

Status: `DRAFT_FROZEN_PENDING_GUARDIAN_REVIEW`. This is a new additive E1 technical preexecution package. It does not override or reclassify CORR01–CORR03; the CORR03 guardian verdict remains `REJECT`. FAST-0 creates no RELEASE or attestation and does not authorize FAST-B1.

## Authority boundary

The immutable machine allowlist is `outputs/gen_enc/GEN_ENC_FAST_START/fast_0_authority_batch_contract_freeze/authority_allowlist.json`. It contains 20 exact, case-sensitive `(path, SHA-256, JSON pointer)` triples and no fixture path. It binds the 20 HAND rows, 20 NEAR rows, sealed RANDOM family object and 20 fixed seeds, sealed PHYSICS family object including parameters and four-step `lhs_algorithm`, its master seed and 20 member-identity seeds, and the complete controlling CAD-0 REV01/REV02 multi-source semantics.

The authorized inspection is reported honestly as `authority_inspection_read=true`: 31 unique non-final authority files were hashed or inspected, 20 semantic allowlist objects were frozen, and the two conformance passes loaded the three sealed family documents six times in total. These reads generated no identity or static result. Final-test stayed sealed and unread.

Two adapters consume the sealed files directly: `scripts/gen_enc_fast_0/adapter_primary.py` and `scripts/gen_enc_fast_0/adapter_independent.py`. They do not import one another or any legacy driver, generator, orchestrator, or CAD mapper. Both implement the frozen SplitMix64 addressing, the binary64 `nextafter(1.0, 0.0)` guard, and PHYSICS Fisher–Yates/LHS independently.

## CAD/static evidence

CAD remains a direct multi-source authority. `cad_multisource_provenance.json` is a mechanical provenance map, not a new authority. Every future persisted member record must carry exact authority triples plus complete ownership cells, all 20 slot placements, volume-root witnesses, all 20 U4 rim/component/shoulder IDs and coordinates, separate general and exception minima, reduced and actual-fluid graphs, and every RANDOM zero-edge witness. No new geometric constant is introduced, and no technical fixture may be promoted.

## Frozen batches and endpoint

The prospective ledger fixes B1 to family ordinals 01–05, B2 to 06–10, B3 to 11–15, and B4 to 16–20. Each batch contains five slots from each of the four families. Their disjoint union is exactly 80. Failed slots remain members; redraw, repair, replacement, reordering, and result-dependent filtering are forbidden.

Each future batch must use one new empty immutable run directory containing 20 identity objects, four explicitly partial family manifests, one batch index, one static audit, one independent verification, `SHA256SUMS.txt`, and exactly one terminal. Publication uses a single atomic commit pointer after validation and verification. A batch is never the final 92-object endpoint. Final integration remains 85 scientific objects, six result objects, and one progress object, in fixed B1→B4 integration order.

Release consumption/revocation is an immutable side record bound to task, batch, run, release, attestation, allowlist, source/schema hashes, command, mode, and roots. Recovery requires durable per-target intent before replacement, an exact target-derived same-parent temp, recomputed ownership token, containment recheck, and real-process termination testing.

## Start and parallel gates

B1 may start only after guardian `ACCEPT`, a distinct task-bound one-use RELEASE/attestation, frozen 20 slots and partial schemas, passing exact adapters, passing single-terminal and real subprocess crash tests, and zero formal/static/final-test counters. After the B1 implementation/semantic checkpoint, B2–B4 may run in parallel only with distinct one-use releases/run IDs, mutually exclusive roots, no shared mutable state, and identical source/schema hashes. Any semantic or invariant defect pauses every uncommitted batch.

No B1 timing or forward pilot is authorized. The default continuation is B2–B4. Timing requires a complete GEN-ENC-2C result seal followed by a separate timing-preflight and GEN-ENC-2 contract.

## Frozen zero-execution state

Formal identity generation: 0. Static-eligibility runs: 0. Formal publication: 0. Simulation, timing, response, endpoint evaluation, comparison, ranking, selection, optimization, COMSOL, full-wave, development, validation and final-test reads: 0. `run_id=null`; DRAFT attempt is 0, non-authoritative, all permissions false, and cannot be flipped or promoted.
