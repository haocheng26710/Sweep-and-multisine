# GEN-ENC FAST B1 PRE-RELEASE-REPAIR-09

Repair-09 is an additive control-path rollover for task `01a049a7-15ca-79e1-92a2-d3822ba8609d`. It does not alter the B1 scientific member set, CAD projection semantics, thresholds, 27-subject verification set, 31-artifact graph, Phase A/B permissions, or final endpoint.

Every successor run must carry three explicitly signed, run-bound relative paths: Phase-A release, guardian attestation, and controller dispatch. The paths are supplied in the signed records and exact CLI; runtime never scans or derives a path to locate controls. Strict schemas and two independent entrypoints enforce the allowed roots, lowercase 64-hex run token in directory and filename, unique filenames, containment, casefold separation, and rejection of predecessor paths/run/dispatch.

The command manifest freezes placeholders for the three external paths. At signing time the controller substitutes only the three signed exact values; the resulting argv is compared byte-for-byte. The predecessor Repair-08 controls remain immutable provenance and are never overwritten or accepted.

This package creates no formal release, attestation, dispatch, run id, authority read, member, static result, handoff, publication, pointer, or package terminal. `final_test_read=false`.
