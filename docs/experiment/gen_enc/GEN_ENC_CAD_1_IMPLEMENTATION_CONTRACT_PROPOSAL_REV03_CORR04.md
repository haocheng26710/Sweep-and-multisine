# GEN-ENC/CAD-1 REV03-CORR04 package-local exact directory-root normalization contract

## Status and evidence ceiling

This is a forward-only incremental preexecution correction addendum. It authorizes no patch, test, Gate, fixture, verifier, package-results, dispatch, task, or run-ID action. Its evidence level is **E0 technical correction only**. It does not replace or silently override any earlier contract, review, result, or scientific conclusion.

The sole authoritative root-cause review is `outputs/governance/RESEARCH_SCOPE_CONTRACTION_AUDIT/reviews/GEN_ENC_CAD_1_E1_ATTEMPT_04_PACKAGE_RESULT_TECHNICAL_INVALIDITY_REVIEW.json` at SHA-256 `b150a2bbf65eda4bb8d54185739e5109ac1d7db634610dc0ee0d45ad12171e61`. Attempt04 remains sealed as `ATTEMPT04_FAIL_CLOSED_PACKAGE_RESULTS_TECHNICAL_INVALIDITY`. Its task, immutable dispatch, and run ID are consumed and must not be reused, edited, reactivated, or treated as an Attempt05 seed. The single atomic failure record remains authoritative and unchanged. Reported Gate A/B, 42/42 expectation matches, and verifier PASS remain unsealed observations, not E1 evidence.

Scientific status remains `NOT_TESTED`; scientific result remains `NONE`; `final_test_read=false`. Formal, scientific, static-eligibility, timing/performance, CAD-kernel, COMSOL, full-wave, physical-experiment, science/development/validation/final-test data, and GEN-ENC-2 permissions are all false.

## Exact defect and frozen correction

The authority string and its established output-root meaning remain byte-for-byte unchanged:

`outputs/gen_enc/GEN_ENC_CAD_1_IMPLEMENTATION_CONTRACT/phase_b/technical/`

The compiler's `package()` currently passes that terminal-slash directory notation directly to the strict path adapter and also appends `"/" + filename`. Splitting either form creates an empty path component. This is a package-local representation conflict, not a defect in the authority, adapter, fixture set, verifier, schema, path containment rules, or scientific semantics.

The only permitted correction is:

1. At the compiler package-success root handling point, compare the dispatch value with the exact authority string above. The accepted external notation must contain exactly one terminal slash and must otherwise be identical.
2. Before any call to the general path adapter, remove only that one terminal slash to derive the internal logical directory path `outputs/gen_enc/GEN_ENC_CAD_1_IMPLEMENTATION_CONTRACT/phase_b/technical`.
3. This operation must be exact suffix removal after exact-string recognition. `rstrip`, generic normalization, separator folding, alternate roots, aliases, case folding, path resolution, allowlist relaxation, or substitution are forbidden.
4. Construct each child only as `normalized_internal_directory + "/" + exact_filename`, producing exactly one separator at the join.
5. The strict general adapter remains unchanged. Empty, dot, dot-dot, repeated-separator, backslash/namespace, unlisted component, and containment violations continue to fail closed.

The exact 11 final filenames, in frozen order, are:

1. `compiled_mapping.json`
2. `static_audit.json`
3. `verification_report.json`
4. `audit_log.json`
5. `source_hash_manifest.json`
6. `schema_hash_manifest.json`
7. `fixture_hash_manifest.json`
8. `complete_run_manifest.json`
9. `result_summary.json`
10. `artifact_inventory.json`
11. `SHA256SUMS.txt`

No success child outside this set may be constructed or persisted. The resulting success root must contain exactly these 11 files and zero subdirectories or unlisted entries.

## Exact future patch boundary

Subject to separate controller authorization after guardian byte review, the recommended exact patch allowlist is:

- `scripts/gen_enc_cad_1_compile_fixture.py` — package-local exact authority recognition, removal of exactly one terminal slash before adapter use, and exact child-path construction only. Current SHA-256: `cde8ca31c398dcb34f65539aeab4c658630f9acef7bb4b607cc0ee70788b8bdd`; 28,824 bytes.
- `tests/test_gen_enc_cad_1_contract_conformance.py` — bounded ordinary falsifiers for this correction only. Current SHA-256: `0bc3d414808ddf69765ba39f127e5fcfc74caeaf22f67bb0e0eec42178d6c6e8`; 17,989 bytes.

Read-only dependency inspection found no additional exact technical dependency requiring a patch. `scripts/gen_enc_cad_1_verify_fixture.py` remains linked and unchanged at SHA-256 `e61ea879cd58694687cf939b536ed1d43be90d12dc8f92b5634df97fdabef6f0`; it declares the same 11 names but does not perform package-success root normalization. Dispatch, schemas, fixtures, manifests, source modules, failure records, prior evidence, and prior reports are immutable. If a later read-only check demonstrates another exact technical dependency, the correction task must list it and stop for controller/guardian scope decision before any edit; it may not expand scope itself.

## Required bounded ordinary falsification gates

After separate patch authorization, only bounded ordinary local tests may run. They must cover all of the following without invoking Gate A/B, the 42 fixtures, verifier main, package-results against an execution dispatch, or any formal/scientific operation:

- **F01 exact authority pass:** the exact authority notation with exactly one terminal slash is accepted and yields the exact internal directory with no terminal slash before the first adapter call.
- **F02 zero/two terminal slash fail:** the zero-terminal-slash form mismatches the exact authority; the two-terminal-slash form is rejected. Neither reaches an adapter write.
- **F03 interior doubled separator fail:** an otherwise similar authority containing any doubled interior separator is rejected; no generic normalization occurs.
- **F04 11-child identity:** the produced ordered child list equals the normalized internal directory plus exactly one separator plus each exact frozen filename; cardinality is 11 and uniqueness is 11.
- **F05 collision:** a pre-existing normalized success root or final child collision fails closed non-destructively, preserving existing bytes and creating no unlisted output.
- **F06 Windows long-path representation:** the normalized logical directory and each child retain identity through the existing extended-length representation; no slash is reintroduced or removed except at the one authorized root boundary.
- **F07 atomic move primitive:** each staged-to-final `replace` uses exact source and exact normalized child destinations, preserves the existing adapter's atomic replace behavior, and rejects mismatch/collision without alternate-path fallback.
- **F08 zero-unlisted package:** a successful bounded package harness observes exactly 11 final file identities, zero unlisted files, zero subdirectories, and removal of only the exact owned staging root after all required moves.

These are technical correction falsifiers only. Passing them does not establish E1, does not recover Attempt04 observations, and does not authorize a new execution attempt.

## Post-correction evidence and stop condition

After an independently authorized two-file patch and bounded ordinary tests, create only the frozen incremental evidence root `outputs/gen_enc/GEN_ENC_CAD_1_IMPLEMENTATION_CONTRACT/recovery_rev03/corr04_incremental_evidence/`, containing exactly seven files and no subdirectories:

1. `corr04_two_file_before_after_manifest.json`
2. `corr04_unchanged_dependency_hash_verification.json`
3. `corr04_static_check_report.json`
4. `corr04_ordinary_test_report.json`
5. `corr04_package_root_falsifier_report.json`
6. `artifact_inventory.json`
7. `SHA256SUMS.txt`

`SHA256SUMS.txt` must contain exactly six non-self entries. Collision or a pre-existing evidence root requires zero writes and a stop. After sealing this evidence, the correction task stops and waits for guardian byte review. No Attempt05 task, dispatch, or run ID may be created as part of correction or evidence sealing.

## Unchanged conclusions and authorizations

Attempt04's atomic failure is preserved. The technical success root and Attempt04 staging remain absent unless a later separately authorized execution creates a new task-bound result. No old conclusion is overridden by default. All formal/science/static/timing/CAD/COMSOL/full-wave/data/GEN-ENC-2 authorizations remain false, as do current source/test patch, ordinary-test execution, Gate, fixture, verifier, package-results, dispatch, run-ID, and Attempt05 permissions.

Current terminal state: `READY_FOR_INTERMEDIATE_CONTROLLER_AND_GUARDIAN_PREEXECUTION_BYTE_REVIEW`.
