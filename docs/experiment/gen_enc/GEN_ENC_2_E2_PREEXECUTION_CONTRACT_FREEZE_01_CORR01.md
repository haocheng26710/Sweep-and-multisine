# GEN-ENC-2 E2 preexecution contract Freeze-01 CORR01

Status: `READY_FOR_B_AND_GUARDIAN_ONE_FREEZE_REVIEW`; E2 execution remains unauthorized.

This is an additive correction to Freeze-01. Freeze-01, its four declared gaps, Advisor B's REVISE review, the guardian ESCALATE review, and the later top-level GAP05 decision remain immutable provenance. CORR01 closes U01–U04 with the supplied top-level rules, mechanically corrects `E2_TECH_01`, and binds the approved GAP05 streaming evidence architecture. It does not overwrite any older file, failure, terminal, result, or conclusion.

## Closed authorities

Angle-to-port excitation uses port angles 0/90/180/270 degrees. For nominal angle theta and frozen nuisance offset delta, theta-effective is `(theta+delta) mod 360`; each nonnegative raw weight is `max(0,cos(theta-effective-port-angle))`, then the four-vector is normalized to L2 norm one with common zero phase. Cardinal angles are exact one-hot. Intermediate angles excite only the two adjacent ports. The same mapping covers all 24 validation angles, the 15-degree bridge, and mandatory 45/135/225/315-degree held-out angles. The five-node/four-branch equations, CAD mapping, R+jωM, and C=V/(rho*c^2) do not change.

Partition seed routing is explicit and has no fallback: development repeats r0/r1 use 2026091001/2026091002; single-use validation repeats r0/r1 use 2026092001/2026092002. Both bind the existing seed-split authority, SplitMix64 rule, and substream registry. Validation cannot open until all 80 development terminals, independent verification, common W, development decisions, hashes, and the development seal are complete. Validation never refits or reopens development.

The neutral throughput reference is the canonical identity at `outputs/gen_enc/GEN_ENC_2_FORWARD_RESPONSE/e2_authority_addendum_01/THROUGH_REFERENCE_U4_IDENTITY_V1.json`. It is not a candidate, fifth family, exact80 member, ranking subject, or endpoint subject. It uses the same model, total connected volume, U4 interface, four ports, central microphone, frequency grid, normalization, central share 0.40, four local shares 0.15, identical external and local-to-central normalized aperture fraction 0.5, loss 0.05, and no local-local/ring/diagonal edge. Candidate and reference throughput are evaluated under identical state/angle/frequency/cell/repeat and physical nuisance before additive sensor noise. A zero or nonfinite reference denominator makes the diagnostic unavailable; no epsilon, point deletion, or reference change is allowed. Throughput remains descriptive.

The single common W is development-only with family mass 1/4, member mass within family 1/20, global member mass 1/80, member-cell mass 1/3675, and equal mass over all two repeats by four states within each member-cell residual block. The expected residual-row count is 2,352,000 and the primary real-embedded feature dimension is 1,664. OAS shrinkage and the existing eigenvalue floor remain unchanged. Missing or nonfinite required input makes W unavailable without deletion, imputation, or renormalization.

## Streaming evidence and storage

The driver writes every complex128 raw chunk in canonical order to the partition-specific temporary root. The independent verifier recomputes each chunk from the same frozen inputs through an independent entrypoint and checks identity/input hashes, shape, dtype, finiteness, `rtol=1e-10`, `atol=1e-12`, sufficient-stat contributions, and merge order. Only after a PASS receipt and finest-resampling-unit derived evidence are durably hashed may raw be released.

Receipts bind partition, identity, chunk, input/code/runtime/dependency hashes, count, shape, dtype, separate driver and verifier raw hashes, maximum absolute/relative difference, sufficient-stat hash, and merge position. Retained schemas preserve common-W covariance accumulators and the finest member/cell/repeat/state/angle/frequency derived values needed to reconstruct every frozen endpoint, bridge, throughput, bootstrap, permutation, and uncertainty report; a global mean alone is forbidden.

For every exact80 identity and each partition, two audit chunks are selected before responses using ascending SHA-256 of `GEN_ENC_E2_RAW_AUDIT_V1|partition|identity_id|chunk_id`; if fewer than two exist, all are retained. Thus the expected permanent raw audit set is 320 chunks. Validation samples remain under the validation root and cannot be read by development. All other PASS raw is deleted immediately after receipt and derived evidence validation. A verifier failure stops the partition, quarantines exactly the failing chunk, performs no later merge, and permits no same-run retry.

Managed working plus retained storage is capped at 48 GiB. Startup requires at least 80 GiB free and valid-empty-or-resume roots; a 32 GiB free reserve is maintained. Any cap or reserve breach is `RESOURCE_BLOCKED`; sealed evidence is not deleted to create space. Driver, verifier, I/O, hashing, merge, checkpoint, and deletion all count inside the unchanged 20 core-hour, 10 wall-hour, and 8 GiB aggregate-memory envelope.

## Mechanical exact80 correction

The exact80 set, order, identity paths, source paths, source hashes, identity hashes, CAD status, and eligibility are unchanged. Only `source_batch` is corrected per family ordinal: 01–05 B1, 06–10 B2, 11–15 B3, and 16–20 B4. A path/batch consistency check is frozen. The prior 60 incorrect labels remain visible only in Freeze-01 provenance.

## Boundaries

Primary/secondary hierarchy, all metrics and thresholds, exact80/full workload, family worst-member rules, negative/unavailable/resource terminals, development/validation isolation, and the E2 reduced-model evidence ceiling remain unchanged. CORR01 does not authorize E2, formal responses, inverse design, search, COMSOL, full-wave, physical experiment, printing, paid services, legacy recomputation, or final-test. No formal run ID exists and every formal response read/write count remains zero.
