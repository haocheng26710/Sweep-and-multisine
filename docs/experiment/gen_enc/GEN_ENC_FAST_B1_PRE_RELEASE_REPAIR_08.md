# GEN-ENC FAST B1 PRE-RELEASE-REPAIR-08

Repair-08 is an additive signing-compatibility correction owned by task `01a049a7-15ca-79e1-92a2-d3822ba8609d`. It creates no formal release, attestation, dispatch, run, authority read, member, publication, pointer, or terminal.

The Phase-A release, guardian attestation, and dispatch schemas require the same four externally supplied Advisor B fields: final-review exact path and SHA-256, plus review-specific immutable-ledger-snapshot exact path and SHA-256. The driver and independent verifier canonical-parse and schema-validate controls, compare all bindings, restrict formal paths to the Advisor reviews and ledger-snapshots roots, and hash the exact bytes before any formal authority access. No scan, live-ledger substitution, local derivation, or future-review constant is accepted.

The guardian attestation schema and runtime share the complete commands, authority allowlist, source/schema/command manifests, and roots binding. Phase-A dispatch freezes `record_kind`, `state`, `phase`, `one_use`, `revoked`, and `final_test_read`, with strict schemas and independent runtime checks.

The missing R07 ledger snapshot is disclosed rather than reconstructed. Task-owned technical fixtures use explicit synthetic review and snapshot records and cannot be mistaken for formal authority.

The scientific contract remains unchanged: mode `FORMAL_B1_EXACT_20_TYPED_CAD`, fixed 20 B1 members, 27 verification subjects, 31 scientific artifacts, the prior claim ceiling, and `final_test_read=false`.
