# GEN-ENC-CAD-1 implementation-contract proposal

Version: `GEN-ENC-CAD-1-PHASE-A-PROPOSAL-v1`

Date: 2026-08-28

Terminal state: `READY_FOR_GUARDIAN_CONTRACT_PREEXECUTION_REVIEW`

Evidence label: `E0_IMPLEMENTATION_CONTRACT_PROPOSAL`

`scientific_evidence_level=NOT_ESTABLISHED`; `scientific_hypothesis_status=NOT_TESTED`; `overrides=false`; `implementation_execution_authorized=false`; `phase_b_started=false`; `formal_generator_invoked=false`; `formal_seed_consumed=false`; `formal_instance_count=0`; `static_eligibility_authorized=false`; `preflight_authorized=false`; `gen_enc_2_authorized=false`; `final_test_read=false`.

## Increment metadata and authority

- **New content:** an implementation-only proposal defining future source, executable-schema, compiler/verifier, static-auditor, falsification-fixture, hash, log and command boundaries for compiling the approved GEN-ENC-CAD-0 REV02 mapping.
- **Relates to:** the approved REV02 contract/package and U1/U2/U3 confirmation; the sealed GEN-ENC-2B source identity; and the blocked GEN-ENC-2C REV02 diagnosis. Machine-readable exact relations are in `relates_to.json`.
- **Unchanged:** all prior files and conclusions; four families; 80 fixed slots; numeric rows, seeds, parameter orders, bounds and DOF; U4 interface; REV02 mappings, exception IDs/coordinates, root proof, thresholds, state meanings and evidence boundary; sealed GEN-ENC-2B bytes and hashes; final-test seal.
- **Evidence:** proposal-only `E0_IMPLEMENTATION_CONTRACT_PROPOSAL`; it is not implementation, fixture evidence, a schema, a CAD result, static qualification, manufacturability or science.
- **Override:** `overrides=false`. This package adds a future compilation contract and does not replace any legacy conclusion. A semantic mismatch must stop and return for renewed user decision.

The user's approval froze only the REV02 E0 declarative semantics. It did not authorize source or executable-schema creation, fixture execution, formal identities, geometry generation, static eligibility, preflight or GEN-ENC-2. This proposal itself creates none of them.

## 1. Exact future file and capability boundary

Only after a guardian `CONTRACT_PRE_EXECUTION_REVIEW` explicitly approves implementation may a separate task create exactly the following implementation sources:

`src/acoustic_encoder/gen_enc/cad_mapping/{__init__.py,api.py,authority.py,canonical.py,compiler.py,connectivity.py,geometry.py,ownership.py,root_solver.py,solid_clearance.py,static_audit.py,volume_accounting.py}`

and exactly these tracked entry points:

`scripts/gen_enc_cad_1_compile_fixture.py`, `scripts/gen_enc_cad_1_verify_fixture.py`.

The independent verifier may use standard-library parsing, hashing and numeric/polyhedral routines but must not import `cad_mapping`, the compiler entry point, any GEN-ENC-2C orchestrator, or a generator API. The compiler may read only a literal synthetic fixture plus the hash-pinned E0 authority. It must not expose seed, row, identity, response, timing, solver, search, optimization or scientific-manifest APIs.

Future executable schemas are limited to:

`schemas/gen_enc/gen_enc_cad_1/{synthetic_fixture.schema.json,compiled_mapping.schema.json,static_audit.schema.json,verification_report.schema.json,audit_log.schema.json}`.

Future tests and literal fixtures are limited to `tests/test_gen_enc_cad_1_contract_conformance.py` and JSON files enumerated by `falsification_fixture_inventory.json` under `tests/fixtures/gen_enc_cad_1/`. This phase creates none of those paths.

The new implementation must neither edit nor silently extend the sealed 42-entry GEN-ENC-2B bundle. Before any future action it must verify `generator_source_sha256=05e35257a101b2542dc03b2d4f5bea07b1d5548a8d2c43efe08722debc32fc02` and `source_bundle_sha256=eded165d2c0bd806efe2c618886a6dbf5c874f45bd3191c0128daa23b887916d`. A mismatch fails before fixture parsing.

## 2. Inputs, outputs, fail-closed order and determinism

The future compiler accepts only: repository root; exact authority-path/hash map; one schema-valid `TECHNICAL_FIXTURE_ONLY_NOT_SCIENTIFIC_IDENTITY` object; output root under a separately approved fixture directory; and an explicit command mode. It outputs only a compiled synthetic mapping, static-audit record, audit log and verification report. No scientific identity-shaped output is allowed.

Fail-closed order is fixed: authorization token and exact command; repository/path containment and no symlink escape; authority and frozen 2B hashes; exact source/schema/fixture bundle hashes; input schema/object-class and forbidden-name audit; canonical-byte round trip; stable-ID uniqueness and exact allowlists; numeric finiteness/bounds; template compatibility and all-family bracket proof; compilation; ownership/union/volume; actual-fluid and reduced-graph audits; solid arrangement/clearance; threshold/state classification; independent verification; output cardinality/hash-chain check. A failure stops later stages and writes only the separately authorized atomic technical failure record.

Determinism requires frozen sector order `000,090,180,270`, family order `HAND_DESIGNED, NEAR_INDEPENDENT, FIXED_SEED_RANDOM_DISORDERED, PHYSICS_METAMATERIAL_INSPIRED`, slot order `SPINE,WINDOW_1,WINDOW_2,WINDOW_3,OUTER_1`, Unicode-code-point key and path ordering, lexicographic cell/face/witness ties, deterministic bisection, and no clock, locale, thread scheduling or platform-dependent branch. JSON is UTF-8 without BOM or trailing newline, recursive Unicode key sort, arrays preserved, shortest round-trip finite binary64 numbers, no insignificant whitespace, and lowercase SHA-256. `NaN`, infinities and negative zero are forbidden; exact decimal authority literals are retained as source strings where needed and converted once under the frozen binary64/runtime rule. Threshold comparison uses binary64 values after derivation, with equality passing. Thresholds and caps are never copied into measurement fields.

## 3. Mapping compilation and version binding

The compiler must reproduce the REV02 authority, not reinterpret it. Coordinate frames, central square/sensor, body, sector primitives, outer steps/collars, cavity/root span, `w(a)=0.002+0.006a`, family-to-slot mapping, disabled slots, ownership/clipping, half-open volume convention and positive-area face connectivity are bound to the exact paths/hashes in `relates_to.json`.

Stable primitive IDs use `FAMILY/MEMBER_OR_FIXTURE/SECTOR/SLOT/COMPONENT` for topology-dependent objects and the immutable REV02 `IFX_U4_*` IDs for fixed exceptions. IDs are assigned before union; fragments receive deterministic suffixes and never inherit exception classification. Each of four sectors retains all five slots, so a four-family formal design would retain 80 member slots; implementation fixtures may model fewer literal members but may not alter the ledger.

For a fixture, derive `q270=-(q0+q90+q180)/3`, local targets from the frozen 0.60 softmax share and central 0.40 share, then solve each cavity length over `[0.002,0.030] m`. Endpoint tests precede deterministic bisection; exact tie selects the lower endpoint; at most 80 iterations; absolute volume residual at most `1e-12 m3`; nonfinite, nonmonotone, unbracketed or exhausted cases fail. The constrained global target interval and four-family endpoint/derivative brackets are compile-time authority checks, not formal row evaluations.

Volume accounting assigns ownership before union, forms sorted elementary half-open boxes, tests exact open-cell membership, sums once in lexicographic order, and records raw volume, overlap deduction, owned union, target and residual. Ownership uses the central half-open plan first, sensor-central precedence, then the minimum sector among outward-dot-product maximizers. Positive cross-owner volume, unresolved ties or nonrectilinear wedge crossing fail.

Actual fluid connectivity is BFS over positive-volume union cells and declared interfaces joined only by strictly positive face area. Reduced graphs are separately calculated, descriptive only. RANDOM zero edges omit paired scientific windows without epsilon or redraw; the common fixed spine/shared plenum remains the physical connectivity floor. Reduced disconnection never means physical disconnection or eligibility failure.

Solid clearance uses the exact octagonal-body-minus-fluid polyhedral arrangement, stable consolidated sheets, support flood from `z=0`, and the eligible opposing-sheet definitions frozen in REV01/REV02. Empty eligible sets are `UNAVAILABLE_FAIL_CLOSED_NOT_INFINITY`. Exact ties sort by distance, sheet IDs and endpoint coordinates. The compiler must report general and exception measurements separately.

## 4. Exact U4 interface exception allowlist

`FIXED_U4_INTERFACE_TRANSITION_EXCEPTION` is permitted only for the 20 stable IDs in `interface_exception_allowlist.json`: four composite rims, their eight `BOTTOM/TOP` component sheets, and eight `LOWER/UPPER` shoulders. Coordinates, counts and participation are exact. Classification by geometry, size, contact, family, overlap or proximity is forbidden.

Rim components witness `interface_exception_min_feature_m=0.0015`; composite rims witness `interface_exception_min_load_path_m=0.0015`. Shoulders witness `interface_exception_min_feature_m=0.0005` and have `t_ff/t_fe=NOT_APPLICABLE`. General proposed measurements remain separately derived: `general_minimum_feature_m=0.002` and `general_minimum_load_path_m=0.002`, compared independently with general gates `0.002` and `0.0016 m`. Unlisted, wrong-ID, coordinate-mismatched, overlap-fragment or topology-dependent use rejects template validity. Exception faces remain in the arrangement and cannot suppress unrelated general witnesses.

## 5. Static qualification states

Template validation occurs before any future formal identity, seed or member evaluation. Missing authority/hash/schema, failed all-domain bracket, incomplete algorithms/allowlists or failed required technical fixtures yields `TEMPLATE_VALIDITY_REJECTED`. It is technical governance, affects zero formal slots, and is not scientific evidence.

Only after a separately authorized future formal stage may member outcomes be `STATIC_IDENTITY_ELIGIBLE`, `COST_INELIGIBLE`, or `GENERATION_TECHNICAL_FAILURE`. A geometry/cost threshold failure is `COST_INELIGIBLE`; source, runtime, schema, hash, canonicalization or algorithm failure is technical. Scientific negativity is impossible in this state machine. All four families and 80 slots remain, with original ID/order/provenance and reason codes. No repair, redraw, replacement, drop, threshold/tolerance change, parameter change or early family termination is allowed. Set states remain the corrected 2C REV01 forms: all eligible -> `SCIENTIFIC_IDENTITY_AND_STATIC_ELIGIBILITY_COMPLETE`; any cost-ineligible and no technical failure -> `IDENTITY_SET_STATIC_ELIGIBILITY_BLOCKED`; any technical failure/missing/duplicate/hash/schema/path/source/runtime failure -> `IDENTITY_SET_TECHNICAL_FAILURE_BLOCKED`.

## 6. Nonformal falsification fixtures

The exact proposed inventory and expected pass/fail are machine-readable. It covers constrained-root extrema, all four family brackets, ownership boundaries and crossings, shared-plenum versus reduced graph, below/equal/above values for both general thresholds, all exception IDs, wrong IDs, spillover, overlap fragments, empty eligible sets, hash/schema/canonicalization failures and repeatability. Expected values must be literal and frozen before implementation; they may not be generated from the system under test. No fixture is executed in this task.

## 7. Hash freeze, verifier, audit log and commands

After separately authorized implementation and fixture passes, freeze exact bytes for authority, implementation source, executable schemas, tests, literal fixtures and command scripts. Create path-sorted per-file hashes and category aggregates, canonicalize the manifest, then hash its exact bytes. Any byte/path/schema/fixture/dependency/runtime/numeric-semantic change invalidates authorization, preserves the previous seal, records an exact diff and requires a new guardian review.

The independent verifier reads authority and produced technical outputs from disk, independently recomputes paths, hashes, canonical bytes, stable IDs, counts, formulas, roots, union accounting, graphs, clearance witnesses and states, and must not trust compiler summaries. Audit logs are append-only logical event arrays in canonical output, with command, runtime, hashes, stage, input/output paths, counts, result and reason; timestamps are metadata excluded from deterministic result identity or omitted.

Proposed future commands, to be run only after explicit approval, are:

1. `python -B scripts/gen_enc_cad_1_compile_fixture.py preflight --fixture <allowlisted-literal-path>`
2. `python -B scripts/gen_enc_cad_1_compile_fixture.py compile --fixture <allowlisted-literal-path>`
3. `python -B scripts/gen_enc_cad_1_verify_fixture.py verify --fixture <allowlisted-literal-path>`
4. `python -B scripts/gen_enc_cad_1_compile_fixture.py package-results --fixture <allowlisted-literal-path>`

Working directory is the repository root. No command accepts a seed, row, member identity or scientific output root.

## 8. Isolation, dependencies, resources and authorization

The implementation phase may use only synthetic/literal fixtures and the frozen declarative authority. It must not access or evaluate formal seeds, formal rows or formal identities, and cannot touch GEN-ENC-2C scientific paths. After this complete proposal receives guardian preexecution approval, a new implementation task is still required; approval does not authorize formal 2C.

Strict sequence: current contract review; explicit implementation authorization; source/schema/literal-fixture creation; static negative-capability and hash preflight; fixture-only execution; independent verification; implementation result seal; new reviewed GEN-ENC-2C executable correction; only then a separate formal-identity authorization. Independent source/schema/fixture authoring may be parallel only after API/path freeze; compilation and verification are serial across a hash barrier. No formal work may run in parallel.

Ordinary local deterministic fixture work is the maximum future resource class. Stop and report before any CAD kernel, solver, COMSOL, full-wave, physical experiment, scientific data, high-resource computation, license, network dependency, destructive operation or semantic change. Acceptance requires all artifacts present, JSON/schema meta-validation, exact relation hashes, zero unlisted files, complete fixture expectations, algorithm/state/authorization consistency, independent verifier separation and all zero counters. The authorization matrix is frozen in `dependency_authorization_matrix.json`.

This task does not contact the guardian and does not commit, push, tag or release. It stops at `READY_FOR_GUARDIAN_CONTRACT_PREEXECUTION_REVIEW` for intermediate-controller acceptance and submission.
