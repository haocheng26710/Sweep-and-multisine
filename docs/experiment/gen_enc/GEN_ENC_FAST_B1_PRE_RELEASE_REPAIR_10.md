# GEN-ENC FAST B1 PRE-RELEASE-REPAIR-10

Repair-10 is an additive, bounded Windows control-path addressability correction for task `01a049a7-15ca-79e1-92a2-d3822ba8609d`. It does not alter the B1 members, CAD projection, metrics, thresholds, evidence ceiling, 27 verification subjects, 31 scientific artifacts, Phase A/B permissions, final 92 endpoint, or final-test seal.

All three Phase-A controls use ordinary `pathlib` at one frozen short root:

`control/gen_enc_b1/phase_a/<full_run_id>/R.json|A.json|D.json`

Guardian/controller-signed records and exact CLI argv explicitly supply and mutually bind all three paths. Runtime discovery, path derivation for location, environment fallback, aliases, traversal, case variants, cross-run mixing, predecessor paths, and revoked R09 run/dispatch identifiers are rejected before any authority read.

The frozen length gate is: current workspace prefix 43, maximum supported prefix 120, relative path 98, current absolute path 142, maximum absolute path 219, and Windows safety limit 259. Any prefix or control path above the frozen budget fails closed before file access. No extended-length adapter is used.

This package creates no formal release, attestation, dispatch, run id, authority read, member, static result, handoff, publication, pointer, or package terminal. `final_test_read=false`.
