# GEN-ENC FAST-B1 PRE-RELEASE-REPAIR-02

Task `01a049a7-15ca-79e1-92a2-d3822ba8609d` closes only the two signing blockers in Advisor B rereview SHA-256 `3f51b22a8e4929dfce88a7de72996ea60e57b4f3b220f146ed3afcc66427f9de`.

The typed CAD projection assigns every semantic leaf to a named selector, type, category, and derived-witness consumer. The technical mirror has 349 semantic leaves. Every other leaf is admitted only by an explicit metadata allowlist entry with a reason. Driver and verifier independently parse the projection and independently derive ownership, slots, volume roots, U4 exceptions, minima, reduced/actual-fluid graphs, and RANDOM zero-edge witnesses from projected values.

No `authority_binding` or model hash is embedded in the CAD witness payload. Per-leaf falsification requires the witness payload to change or the typed adapter to fail closed. The driver, verifier, and their CAD adapters import neither the old B1 semantic modules nor each other; the only shared module contains canonical JSON, hashing, JSON Pointer, and atomic-write primitives.

The projection contract file hash, dependency-matrix file hash, and dependency-structure hash are required bindings for future RELEASE, guardian attestation, ISSUED/CONSUMED records, run-id context, batch index, member provenance, and independent verification.

Status: `READY_FOR_INTERMEDIATE_AND_ADVISOR_B_RE_REVIEW_FORMAL_EXECUTION_BLOCKED`.

