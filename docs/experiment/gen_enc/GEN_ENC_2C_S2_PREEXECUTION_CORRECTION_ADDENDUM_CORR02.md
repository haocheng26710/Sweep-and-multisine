# GEN-ENC-2C S2-CORR02 technical preexecution correction

Addendum metadata: new CORR02 release-gated entrypoints, direct-schema endpoint, independent verifier, publication recovery and immutable BF ledger; relates to the sealed CORR01 fail-closed package and guardian result-seal review; all S1, original S2 and CORR01 bytes are unchanged; evidence level `E1_TECHNICAL_PREEXECUTION_CORRECTION_ONLY`; `overrides=false`.

Task binding is exactly `01a048bd-dc5e-7e93-87e6-72d299ebaa4d`. No formal row, seed, data or target is read or created in this increment. The only direct correction authority is `GEN_ENC_2C_S2_CORR01_RESULT_SEAL_REVIEW.json`, SHA-256 `e50c05875fb26116f71e2e4142ce2f13f415f1c4c8095377a4be3878374c6e1d`.

CORR02 restores the original guardian S2-BF-01 through S2-BF-11 text and categories without reassignment. The driver builds the exact S1-ordered 80+4+1+6+1 direct-schema tree. Technical substitute mode writes no wrapper around formal-schema objects; substitute authority provenance is explicit and cannot be promoted. Formal mode is unreachable until a new canonical guardian RELEASE passes task, expiry, revocation, permissions, runtime, commands/modes, contract/manifests, actual guardian-review bytes, one-way attestation and four exact authority path/hash/pointer gates.

The release record has `run_id=null` and binds only root bases, avoiding a hash fixed point. After the immutable RELEASE full-file SHA-256 exists, driver and verifier independently derive the run ID from domain, release SHA, task ID and source/schema/fixture/authority/allowlist hashes joined by byte `0x0A`, with no trailing newline; literal bytes `5c6e` are a negative falsifier. A later canonical one-way attestation binds that derived run ID and the five expanded run-specific roots. DRAFT is expired, never reusable or promotable, all execution permissions false, and requested scope is non-authoritative.

CAD/static values are parameter-derived using the approved 0.40/0.60 split, softmax sector shares, exact family slot mappings, deterministic 80-step roots, U4/fixed exceptions, minimum-feature/load-path witnesses, RANDOM exact-zero reduced edges and actual positive-area shared-plenum BFS. The verifier imports no driver, generator, orchestrator or CAD mapper and independently regenerates all 80 parameters and CAD/static states.

Publication uses the sealed S1 allowlist as its sole ordered mapping, including the exact `docs/progress/GEN_ENC_2C_SCIENTIFIC_IDENTITY_GENERATION_RESULTS.md` target. The transaction journals PREPARED/PUBLISHING/COMMITTED/ROLLED_BACK, inventories only transaction-owned `.c2tmp` files, uses copy/fsync/same-directory replace, and rolls back copy, fsync, replace, journal and cross-root progress faults to zero published targets. Startup recovery rolls back PREPARED/PUBLISHING journals and leaves unrelated files untouched. Package-results accepts exactly one terminal marker.

The frozen formal sequence is release-preflight, driver generation/static audit, independent verifier, then package-results. Any predecessor failure stops the sequence. A formal run consumes its one-use RELEASE; a retry requires a new guardian RELEASE, dispatch ID, full-file SHA, derived run ID and disjoint run-specific staging/success/failure/temp/journal roots. Stale, cross-attempt, consumed, revoked, expired or mismatched pairs fail closed.

The direct authority bindings remain the four sealed S2 inputs: slot ordering `b7381875...de9a`, family design tables `3b7d649f...f751`, family identity manifest `e09bcd22...2ee6`, and CAD static authority `a077819b...5b13`. CORR02 did not open these bytes. A future RELEASE must bind the exact paths, full hashes and reviewed JSON pointers, and both CLIs compare those bindings before any authority read.

Permanent disclosures remain unchanged: CORR01 test history `14/8`, `19/3`, `22/0`, `23/1`, three `24/0`, seal `24/0`; CORR01 eight roots 5280 files/726 directories; old S2 three roots 21/21; S1 six roots 44/58; CORR04 residue; S1 ACL and AST correction history. No residue was cleaned. Four-state, held-out, continuous-angle, inverse-design, NO_IMPROVEMENT and full-wave obligations remain downstream.

CORR02 ordinary history is `3 pass/4 fail/15 error`, `18/4`, `18/4`, `21/1`, focused `0/1`, focused `1/0`, `22/0`, `24/0`, `26/0`, first seal `26/0`, then post-contract-interface seal `26/0`. The early failures exposed incomplete authority fixtures/schema references/recovery, Windows path length and an allowed-command mismatch; all are retained as technical iteration provenance. The package residue inventory lists every new root and count; none was cleaned.
