# GEN-ENC FAST-B1 preexecution executable freeze addendum

Status: `DRAFT_FROZEN_PRE_RELEASE`. Task: `01a049a7-15ca-79e1-92a2-d3822ba8609d`. This additive sidecar does not modify FAST-0, CORR01–CORR03, any prior report, or any formal authority. It creates neither a RELEASE nor a guardian attestation and authorizes no formal read, identity, static run, publication, timing, response, performance, full-wave, physical, final-80, final-92, or final-test action.

The sole accepted upstream authority is `GEN_ENC_FAST_0_AUTHORITY_BATCH_CONTRACT_FREEZE_PREEXECUTION_REVIEW.json`. The delegation text mechanically rendered its SHA with a non-hexadecimal `r`; the actual file bytes, FAST-0 ledger, and `Get-FileHash` agree on the valid SHA256 `f96a71e850ceaefa9c2267c2641d6307c9c98af38b53aa8ef3893fef5de2c8fd`. This addendum binds the actual bytes and records the discrepancy without changing upstream material.

## Frozen B1 membership and evidence ceiling

B1 is exactly `HAND_01`–`HAND_05`, `NEAR_01`–`NEAR_05`, `RANDOM_01`–`RANDOM_05`, and `PHYSICS_01`–`PHYSICS_05`, in that family and ordinal order. Every slot remains in the denominator after technical failure, cost ineligibility, template rejection, or unavailability. Redraw, repair, replacement, reorder, and result-dependent filtering are forbidden.

The maximum future B1 evidence is batch-local identity provenance, complete member-local CAD/static status, technical validity, and independent verification. Family/global claims, cross-family comparisons, performance, matched-cost claims, direction readability, `NO_IMPROVEMENT`, timing, full-wave/physical claims, and final-80/final-92 completion are forbidden.

## Executables and exact authority boundary

The formal driver is `scripts/gen_enc_b1/formal_driver.py`; the verifier is `scripts/gen_enc_b1/independent_verifier.py`. They do not import one another. Neither formal CLI exposes `synthetic`, `technical`, arbitrary-triple, or hidden alias modes. The exact executable command order is `preflight`, `generate-staging`, independent `verify`, `publish`, `recover`, and `package-results`; full argv arrays are hash-bound by `command_manifest.json` and must be byte-identical in the future RELEASE.

Formal reads accept only the 20 exact FAST-0 path/SHA256/JSON-pointer triples. Before any authority read, the driver/verifier check canonical RELEASE and one-way attestation bytes, actual task and B1, derived run ID, expiry, one-use state, revocation absence, exact authority/source/schema/command hashes, exact mode and permissions, mutually exclusive contained roots, and immutable `ISSUED`/`CONSUMED` records. DRAFT attempt 0 is non-authoritative, has a null run ID and all permissions false, and cannot be flipped or promoted. A future RELEASE must be newly created canonical bytes at signing time.

The run ID is frozen as `sha256(b"GEN-ENC-FAST-B1-RUN-ID-v1\n" + canonical_json(release_with_run_id_replaced_by_64_zeroes))`. Thus task, batch, authority, manifests, commands, roots, expiry, and all other release fields affect the run ID without a self-hash cycle.

## Complete member and package semantics

Each of 20 member identities persists exact family/slot provenance and a complete CAD/static object containing five ownership cells, twenty slot placements, four independently checkable volume-root witnesses, twenty exact U4 exception IDs with coordinates, candidate-derived general and exception minima, the six-edge reduced graph, four-edge actual-fluid graph, and the exact RANDOM zero-edge witness list. Measured minima are recomputed from candidate lists; `thresholds_copied_as_measurements=false` is mandatory. Missing, extra, empty, reordered, copied-threshold, seed, axis, graph, or witness defects fail closed.

The immutable run directory contains exactly 31 artifacts: 20 members, four partial family manifests, batch index, static audit, independent verification, SHA256SUMS, generation terminal, verifier terminal, and publication terminal. The partial manifests are explicitly five-of-twenty and never complete-family manifests. The independent verifier recomputes named axes, row and seed bindings, RANDOM binary64 nextafter behavior, PHYSICS Fisher–Yates midpoint LHS, all CAD/static equations and cardinalities, then reseals dependent hashes. Generation remains non-success; no commit pointer exists before verification.

## Publication, recovery, and terminal order

Publication uses one durable journal intent before every one of 31 replaces. Every temporary path is exactly target-derived; recovery recomputes its ownership token from run ID, target index, absolute target and absolute temp. Containment and reparse ancestry are rechecked immediately before every deletion. Journal booleans and filename substrings are never deletion authority.

After all 31 targets are durable and verified, one commit pointer is atomically written in a disjoint pointer root. Only then may exactly one schema-valid package terminal be written. A pointer/terminal failure rolls back the owned pointer and all published targets, reporting `published=0`. Success and failure terminals are mutually exclusive; success forbids later failure. The technical suite kills a real subprocess after replace and before journal update for each of the first twenty member targets, and covers copy, fsync, replace, journal, pointer, and terminal faults.

## Release boundary

This freeze ends at `READY_FOR_INTERMEDIATE_AND_ADVISOR_B_REVIEW_FORMAL_EXECUTION_BLOCKED`. Advisor B may review ordinary technical conformance. Only the intermediate controller may return after advisor-B and guardian signing. No authority content, formal row/seed/CAD object, formal identity, static audit, publication target, RELEASE, attestation, commit pointer, or success terminal was read or created in this stage.
