# GEN-ENC-2 E2 FREEZE02-RC-B-FINAL

## Last preexecution correction

This is `LAST_PREEXECUTION_CORRECTION`, the final permitted GEN-ENC-2 E2 preexecution correction and numbering event. No later Freeze or RC package may be opened. After final confirmation, the project should enter E2 with the frozen disclosures. An ordinary non-scientific defect encountered during formal execution may receive at most one bounded formal-execution repair; it does not reopen preexecution science or governance.

This additive package binds Guardian review SHA-256 `b5f982cf2c97bb17eb0d14a4922b52752e876610724c5e6321820c21b39b1483` and Advisor B provenance SHA-256 `53a613a9af66b2787d557788924ea31391d5beacab71d8c289d89943054f0e4a`. RC-A/C/D/E and GAP01–GAP05 remain closed and unchanged. Exact80, workload, formulas, thresholds, partitions, resource limits and evidence ceiling are unchanged.

## RC-B final implementation

Candidate reconstruction now begins from exactly 147 phase-correct full-role manifests and consumes every required role. Development consumes nine roles; validation consumes the same nine plus bridge-24 and held-out-4. Each payload path, SHA-256, shape and dtype is verified. A missing role, extra role, missing manifest, broken reference mapping, receipt mismatch or payload mismatch fails closed.

Outputs are separately persisted and hash-bound for primary `E_primary`, `r_stable` and stable-rank eligibility; secondary-band non-substitution disclosure; shared/differential summaries; matched-cost eligibility; all-256-frequency descriptive throughput and unavailable state; 24-angle bridge; four held-out angles; finest-unit bootstrap/permutation inputs and empirical uncertainty; terminal-reduction inputs; candidate terminal; exact-20 family worst-member terminal; and validation global terminal/ranking after all four families complete.

Candidate outcomes include PASS, NEGATIVE, UNAVAILABLE, COST_INELIGIBLE, BRIDGE_FAIL and TECHNICAL_FAILURE. Development does not fabricate validation evidence: its bridge, held-out and global outputs are explicitly `NOT_EVALUATED_DEVELOPMENT`, with no development ranking. Validation alone seals bridge, held-out and global outputs.

Reference denominator and availability payloads move to one canonical partition index. Every index origin binds partition/key, original manifest and role path/hash, PASS receipt path/hash, canonical payload path/hash and identity. Reconstruction resolves moved paths only through this index and verifies the full chain. The index is durable and hashed before the existing RC04 raw deletion gate.

The implemented role payloads are unchanged. The canonical index uses the already reserved 96,337,920-byte dedup-record allocation, so the mechanical bound remains 45,598,701,824 bytes (42.467100381851196 GiB), below 48 GiB.

No formal command ran, no formal root/control/run ID exists, no response was read or written and `final_test_read=false`.

