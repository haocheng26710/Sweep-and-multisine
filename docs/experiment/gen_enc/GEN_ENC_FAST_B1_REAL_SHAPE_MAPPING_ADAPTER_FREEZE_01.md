# GEN-ENC FAST B1 real-shape mapping adapter freeze 01

This is the single additive mapping-adapter correction authorized after the successful R11 collector.

`GEOMETRY_VOLUME` and `GEOMETRY_TARGET_FRACTION` are derived from the frozen numeric identity manifest and cross-checked against the corrected central union-volume formula. `GEOMETRY_SLOT_Z_INTERVAL_M` preserves the complete two-endpoint collector and per-slot authority intervals. The historical scalar slot-plane expectation is superseded in additive provenance only.

Static consumers must use finite ordered endpoints, containment, non-empty overlap, and endpoint-difference thickness/clearance. A midpoint or selected face cannot enter authority, identity hashes, eligibility, ranking, metrics, or scientific evidence.

The unchanged pointers for `ROOT_BRACKET`, `SLOT_ROWS`, `U4_PARTICIPATION`, and `ZERO_EDGE_RULE` now expect their observed object, object, object, and string types. `OWNERSHIP_CELLS` and `OWNERSHIP_RULE` remain future outputs; five witnesses per member are derived from the existing transforms, sector order, and ownership rules.

The adapter covers B1 exact 20 across HAND, NEAR, RANDOM, and PHYSICS. It changes no threshold, metric, evidence ceiling, or final-test rule. No identity/static/scientific execution occurred during freezing.
