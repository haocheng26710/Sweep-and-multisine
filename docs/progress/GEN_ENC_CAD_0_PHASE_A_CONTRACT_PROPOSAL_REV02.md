# GEN-ENC-CAD-0 Phase A contract proposal REV02

Date: 2026-08-28

Terminal state: `READY_FOR_GUARDIAN_REVIEW_BEFORE_USER_CONFIRMATION`

REV02 is an incremental documentation/machine-contract correction for guardian RC-01..RC-06. Initial and REV01 artifacts remain unchanged.

The four common collar exceptions are now fully enumerated: four stable composite 1.5 mm rim IDs, eight top/bottom rim sheets and eight stable 0.5 mm transition-shoulder IDs. They use the dedicated `FIXED_U4_INTERFACE_TRANSITION_EXCEPTION` classification and are excluded from general aggregates only by exact ID. Exception minima are separately derived and witnessed; no exception can apply to topology or family-dependent primitives.

The local-target proof is corrected to the constrained domain `q270=-(q0+q90+q180)/3`. Exact global witnesses are `(0.12,0.12,0.12)` for the minimum derived-sector target and `(-0.12,-0.12,-0.12)` for the maximum. The resulting interval remains `[3.7578617934992e-6,5.38393583634402e-6] m3`, so the four-family root bracket remains complete.

No user decision is requested. U1/U2/U3 retain the exact guardian-recommended semantics and require another guardian review first.

`phase_b_started=false`; `formal_generator_invoked=false`; `formal_seed_consumed=false`; `formal_instance_count=0`; all authorization fields are false; all execution/generation/scientific-read counts are zero; identity hashes are null; `final_test_read=false`.
