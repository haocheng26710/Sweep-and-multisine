# GEN-ENC-CAD-1 Phase A implementation-contract proposal

Date: 2026-08-28

Terminal state: `READY_FOR_GUARDIAN_CONTRACT_PREEXECUTION_REVIEW`

This increment adds only an E0 implementation-contract proposal and machine-readable governance package for compiling the user-approved GEN-ENC-CAD-0 REV02 declarative mapping. It relates forward to REV02/U1-U3, the immutable GEN-ENC-2B source seal and the blocked GEN-ENC-2C authority diagnosis. It changes no legacy file or conclusion and `overrides=false`.

The proposal freezes future file/API/CLI boundaries, deterministic canonicalization, stable IDs, four-family slot mappings, constrained roots, ownership, Boolean volume accounting, shared-plenum versus reduced connectivity, solid-clearance audits, the exact 20-ID U4 exception allowlist, template/member/set state machines, literal falsification fixtures, independent verification, hash invalidation, audit logs, dependencies, resource stops, acceptance and authorization.

No implementation source, executable schema or fixture was created or run. No generator, formal seed, row, identity, geometry, CAD kernel, eligibility, response, timing, search or optimization was invoked. Development, validation and final-test scientific data were not accessed.

`implementation_execution_authorized=false`; `phase_b_started=false`; `formal_generator_invoked=false`; `formal_seed_consumed=false`; `formal_instance_count=0`; `static_eligibility_authorized=false`; `preflight_authorized=false`; `gen_enc_2_authorized=false`; `final_test_read=false`.

The user's prior approval froze E0 semantics only. A guardian contract-preexecution approval and a separate implementation task are required before source/schema creation or literal-fixture execution. Formal GEN-ENC-2C remains a later, separately reviewed stage. Await intermediate-controller acceptance; do not contact guardian or commit/push/tag/release.
