# GEN-ENC-CAD-0 Phase A contract proposal REV01

Date: 2026-08-28

Terminal state: `READY_FOR_GUARDIAN_REVIEW_BEFORE_USER_CONFIRMATION`

This incremental revision preserves the initial proposal unchanged and addresses guardian findings F-01..F-09. It is still `E0_CAD_MAPPING_CONTRACT_PROPOSAL`, not geometry, static eligibility or scientific evidence.

Key correction: the literal sealed `0.0092 m` port height plus two `0.0016 m` load paths requires `0.0124 m`, exceeding the `0.0122 m` device cap. Corrected T1 therefore openly proposes a bounded side-interface collar exception and an `0.0082 m` internal fluid height with derived `0.002 m` top/bottom covers. This new semantic is part of future U1 and is not silently assumed.

The revised algebra uses central square half-side `0.019073321232601637 m`, root span `0.058926678767398356 m`, fixed local volume `2.1848e-6 m3`, cavity area `1.054e-4 m2` and `L in [0.002,0.030] m`. Family-by-family bound-corner proofs bracket the conservative sealed target interval `[3.7578617934992e-6,5.38393583634402e-6] m3`; the minimum derivative is `9.66e-5 m2`. No formal row or seed was evaluated.

T2 and D4 have been removed from the choice matrix. Member `COST_INELIGIBLE`, complete 80-slot retention and no repair/redraw/replacement/drop remain binding. `TEMPLATE_VALIDITY_REJECTED` is available only before formal execution when proofs or nonformal fixtures fail.

The future matrix is limited to U1 corrected T1, U2 shared-plenum D2-A and U3 reserved-capacity D3-A, each approve versus reject/new proposal or escalation. Current user confirmation remains forbidden pending a new guardian review.

All authorization fields are false; all execution/generation/scientific-read counts are zero; identity hashes are null; final-test remains sealed/read false. No initial or legacy file was modified.
