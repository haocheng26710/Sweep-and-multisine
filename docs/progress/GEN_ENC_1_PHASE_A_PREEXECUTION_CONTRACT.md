# GEN-ENC-1 PHASE A 预执行合同报告

日期：2026-08-27
终态：`GEN_ENC_1_PHASE_A_CONTRACT_READY_FOR_GUARDIAN_REVIEW`
`execution_authorized=false`，`final_test=sealed`，`final_test_read=false`
覆盖旧结论：`false`

## 结果

本回合只完成 E0/E1 方法合同：冻结 `H=A C B` 可观察等价类和不可识别边界、`Y/W` 与稳定秩估计器、等权 nuisance 和非插值 5% 分位数、三层判定、gauge-invariant continuous bridge、matched-cost schema、四 family identity schema、依赖/资源/反证与 active RQ 映射。GEN-ENC-0R 两次审查的 binding disclosures 全部继续生效。

没有产生 E2 拓扑性能结论。数值 endpoint、拓扑实例、模拟、参数搜索、优化、分类器训练、实验、COMSOL、旧结果重算和 final-test 读取均为 0。

## 阻塞项

当前无科学依据填实的数值均保留为 `REQUIRES_USER_OR_STAGE_SPECIFIC_VALUE`：频带端点/网格点数、材料或有效声学体积 cap、端口与传感器 counts、几何包络与接口尺寸、插损/传输 caps、候选评估/CPU/内存/wall-clock caps、制造分辨率与参数自由度 cap，以及每 family 的 instance/ensemble count、生成分布、seeds、版本与哈希。任何一项未填实都会阻塞 GEN-ENC-2。

## 判定边界

`E_primary>1.0` 仅是白化噪声单位工程门槛，不是统计显著性、分类准确率或部署性能。candidate、family 与 global 判定分离；一个 baseline 失败不否定整个 GEN-ENC，一个候选通过不建立 topology-general law。E1 只允许数学等价/可识别性与方法规范主张；E2 限于预注册实例/ensemble，E3 限于 2–3 个合格 anchors，均不得提升为实体或拓扑普遍物理事实。

## 发现与后继

旧报告和旧机器产物只读；新产物位于 `outputs/gen_enc/GEN_ENC_1_IDENTIFIABILITY_AND_ESTIMATOR/`。`bidirectional_discovery_established=false` 持续披露。SUP-3 和 TRANS 重闭合不是依赖。PHASE A 完成后停止；只能由上级主控提交守门审查并另行发送 PHASE B 授权，不能自行启动 GEN-ENC-2。
